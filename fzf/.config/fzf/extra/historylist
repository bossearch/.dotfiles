# fh - repeat history
fh() {
  print -z $(
    ([ -n "$ZSH_NAME" ] && fc -l 1 || history) | \
    fzf +s --tac --preview-window hidden | \
    sed -E 's/ *[0-9]*\*? *//' | \
    sed -E 's/\\/\\\\/g'
  )
}
